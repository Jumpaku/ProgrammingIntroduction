version: "4"
services:
  site:
    command: jekyll serve
    image: jekyll/jekyll:latest
    environment:
      - JEKYLL_ENV=production
    volumes:
      - .:/srv/jekyll
      - ./vendor/bundle:/usr/local/bundle
    ports:
      - 4000:4000
      -   80:4000
