package jumpaku.programming.operations

fun main(args: Array<String>) {
    val t = true
    val f = false
    println("t && f = ${t && f}") // かつ
    println("t || f = ${t || f}") // または
    println("!t = ${!t}") // 否定
    println("!f = ${!f}") // 否定
}
